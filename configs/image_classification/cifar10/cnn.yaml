name: cifar10_cnn_seed42
model: cnn
model_config:
  in_channels: 3
seed: 42
batch_size: 128
learning_rate: 0.001
epochs: 50
num_workers: 0
val_fraction: 0.1
data_dir: data
checkpoint_dir: checkpoints
results_dir: results
