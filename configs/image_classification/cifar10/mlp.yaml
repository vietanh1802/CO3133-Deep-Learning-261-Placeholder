name: cifar10_mlp_seed42
model: mlp
model_config:
  input_dim: 3072
  hidden_dims: [256, 128]
  dropout: 0.2
seed: 42
batch_size: 128
learning_rate: 0.001
epochs: 50
num_workers: 0
val_fraction: 0.1
data_dir: data
checkpoint_dir: checkpoints
results_dir: results
