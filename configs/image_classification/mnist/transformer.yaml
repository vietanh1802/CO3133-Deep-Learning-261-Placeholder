name: mnist_transformer_seed42
model: transformer
model_config:
  token_dim: 28
  embedding_dim: 128
  num_heads: 4
  num_layers: 2
  dropout: 0.1
seed: 42
batch_size: 128
learning_rate: 0.001
epochs: 50
num_workers: 0
val_fraction: 0.1
data_dir: data
checkpoint_dir: checkpoints
results_dir: results
